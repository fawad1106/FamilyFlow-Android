# LifeOS

A personal command center for tasks, projects, notes, deadlines, and files.

## Run locally

```bash
python -m pip install -r requirements.txt
python -m uvicorn backend.app.main:app --reload
```

Open http://127.0.0.1:8000
